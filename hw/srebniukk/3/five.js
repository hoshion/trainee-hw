import create from 'prompt-sync';
const prompt = create();

export function five() {

    let color = ['red', 'blue', 'orange', 'pink'];
    let tea = new Array('black', 'green');
    let books = new Array(5);

}
